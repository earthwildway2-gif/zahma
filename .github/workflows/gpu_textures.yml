name: GPU textures (free Kaggle GPU)
on:
  workflow_dispatch:
    inputs:
      kaggle_username:
        description: "Kaggle username (public, as in kaggle.com/<username>)"
        required: true
      script:
        description: "which script in gpu/ to run"
        required: true
        default: textures.py
permissions:
  contents: write
jobs:
  gpu:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    env:
      KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
      KUSER: ${{ inputs.kaggle_username }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -U kaggle
      - name: Prepare the kernel
        run: |
          test -n "$KAGGLE_API_TOKEN" || { echo "Missing KAGGLE_API_TOKEN secret"; exit 1; }
          mkdir kernel && cp "gpu/${{ inputs.script }}" kernel/script.py
          cat > kernel/kernel-metadata.json <<META
          {"id": "$KUSER/zahma-gpu-job", "title": "zahma-gpu-job", "code_file": "script.py",
           "language": "python", "kernel_type": "script", "is_private": "true",
           "enable_gpu": "true", "enable_internet": "true", "dataset_sources": [], "competition_sources": [], "kernel_sources": []}
          META
      - name: Push (this starts the GPU run)
        run: kaggle kernels push -p kernel
      - name: Wait for the GPU run to finish
        run: |
          for i in $(seq 1 140); do
            S=$(kaggle kernels status "$KUSER/zahma-gpu-job" 2>&1 || true)
            echo "$i: $S"
            case "$S" in
              *COMPLETE*) exit 0;;
              *ERROR*|*CANCEL*) echo "Kernel failed"; kaggle kernels output "$KUSER/zahma-gpu-job" -p out || true; ls out || true; exit 1;;
            esac
            sleep 60
          done
          echo "Timed out"; exit 1
      - name: Download and commit the results
        if: always()
        run: |
          kaggle kernels output "$KUSER/zahma-gpu-job" -p out || true
          mkdir -p assets/textures assets/models gpu_out
          cp out/*.jpg assets/textures/ 2>/dev/null || true
          cp out/models/*.glb assets/models/ 2>/dev/null || true
          cp out/log.txt gpu_out/log.txt 2>/dev/null || true
          git config user.name "zahma-bot"; git config user.email "zahma-bot@users.noreply.github.com"
          git add assets gpu_out && git commit -m "GPU job results" || echo "nothing to commit"
          git pull --rebase origin main && git push origin HEAD:main
